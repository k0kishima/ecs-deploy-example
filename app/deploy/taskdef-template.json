{
  "containerDefinitions": [
    {
      "name": "web",
      "image": "${IMAGE_URI}",
      "essential": true,
      "memory": 512,
      "portMappings": [
        {
          "containerPort": 80,
          "hostPort": 80,
          "protocol": "tcp"
        }
      ]
    }
  ]
}
